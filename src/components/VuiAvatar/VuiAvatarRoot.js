import Avatar from "@mui/material/Avatar";
import { styled } from "@mui/material/styles";

const VuiAvatarRoot = styled(Avatar)(() => ({
  width: 40,
  height: 40,
}));

export default VuiAvatarRoot;
